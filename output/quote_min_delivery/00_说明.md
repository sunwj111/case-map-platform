# 家装报价最小交付表（已生成）

从现有 `configs/quote.json`、接口清单、功能点地图样例抽出，可直接用 Excel 打开。

| 文件 | 用途 |
|---|---|
| 01_节点上下游.csv | 流程全景连线 |
| 02_功能点_接口.csv | 功能点 → 接口 |
| 03_用例_功能点_接口.csv | 用例 → 功能点 → 接口（当前为样例 8 条） |
| 04_接口下游.csv | 接口 → BOM/房源/MQ 等 |

空白表头在 `input/quote/templates/`。历史用例请放到 `input/quote/历史用例.xlsx`。
